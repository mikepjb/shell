" -----------------------------------------
" Spartan config
" -----------------------------------------

set nocompatible " Disable VI Compatibility
syntax on
filetype plugin indent on

set autoindent
set backspace=indent,eol,start
set formatoptions-=t              " Don't add line-breaks for lines over 'textwidth' chars
set hlsearch
set incsearch                     " cursor follows matches as you /search
set ignorecase
set nonumber
set nostartofline
set showmatch
set showcmd                       " bottom right of screen shows partially-typed commands i.e 'g'
set textwidth=80                  " 80 columns
set fillchars=vert:│
set belloff=all
set clipboard=unnamed,unnamedplus
set nowritebackup
set noswapfile
set tabstop=2                     " Number of spaces <tab> counts for
set softtabstop=2
set shiftwidth=2
set expandtab                     " Space > Tabs
set colorcolumn=+1
set nowrap
set path+=**                      " expands find results in a fuzzy manner
set gdefault                      " replaces all matches, not just the first
set re=2                          " re=1 by default, typescript hl is slow without this
set statusline=%<%f%*\ (%{&ft})\ %-4(%m%)%=%-19(%3l,%02c%03V%)
set laststatus=2
let &l:errorformat = '%f:%l:%c:\ %m,%f:%l:\ %m,%-G%.%#'

set undodir=/tmp/.vim-undo-dir undofile
if !isdirectory(&undodir)
  call mkdir(&undodir, "", 0700)
endif

let [g:omni_sql_no_default_maps, g:sh_noisk] = [1, 1] " prevent remaps

if executable('rg') " N.B ripgrep searches all files by default
  set grepprg=rg\ --vimgrep\ --smart-case\ $*
  set grepformat=%f:%l:%c:%m,%f:%l:%m
endif

colorscheme spartan

nnoremap Y y$
nnoremap <C-j> <C-w>j
nnoremap <C-k> <C-w>k
nnoremap <C-h> <C-w>h
nnoremap <C-l> <C-w>l
inoremap <C-l> <space>=><space>
inoremap <C-c> <esc>
nnoremap S <C-^>
nnoremap <C-q> :q<CR>
nnoremap <C-g> :noh<CR>:redraw!<CR><C-g>
nnoremap gd <C-]>
nnoremap gD g<C-]>
nnoremap gi :e ~/.vimrc<CR>
nnoremap gn :e ~/.notes/index.md<CR>
nnoremap ge :e <C-R>=fnamemodify(resolve(expand('%:p')), ':h') . '/'<CR>
nnoremap gs :grep<space>
nnoremap gc :!ctags -R .<CR>
nnoremap gr :read !snip<space>
nnoremap gl :cclose<CR>
nnoremap Q @q
xmap ga <Plug>(EasyAlign)
nmap ga <Plug>(EasyAlign)
nnoremap <A-n> :cnext<CR>
nnoremap <A-p> :cprevious<CR>
cnoremap        <C-A> <Home>
cnoremap        <C-B> <Left>
cnoremap <expr> <C-D> getcmdpos()>strlen(getcmdline())?"\<Lt>C-D>":"\<Lt>Del>"
cnoremap <expr> <C-F> getcmdpos()>strlen(getcmdline())?&cedit:"\<Lt>Right>"
nnoremap Z :echo map(synstack(line('.'), col('.')), 'synIDattr(v:val, "name")')<CR>
nnoremap <A-t> :cexpr system('test-this ' . expand('%'))<cr>
nnoremap <A-T> :cexpr system('test-this')<cr>
nnoremap <A-c> :cexpr system('lint-this "' . expand('%') . '"')<cr>
nnoremap <A-C> :cexpr system('lint-this "' . expand('%') . '" --fix')<cr>

augroup base
  autocmd!
  autocmd BufEnter * :syntax sync fromstart
  autocmd QuickFixCmdPost [^l]* nested cwindow
  autocmd FileType clojure packadd vim-fireplace
  autocmd FileType go,java setl ts=4 sw=4 sts=4
augroup END

command! SudoWrite :w !sudo tee > /dev/null %
command! TrimWhitespace :%s/\s\+$//
command! GenerateHelpTags :helptags ALL

function! SelectaCommand(choice_command, selecta_args, vim_command)
  try
    let selection = system(a:choice_command . " | selecta " . a:selecta_args)
    let selection = substitute(selection, ' ', '\\ ', "g")
    redraw!
    exec a:vim_command . " " . selection
  catch /Vim:Interrupt/
    redraw!
    return
  endtry
endfunction

nnoremap <space> :call SelectaCommand("fd -t f . .", "", ":edit")<cr>
nnoremap <A-o> :call SelectaCommand("fd -t f . ~/.notes/", "", ":edit")<cr>
